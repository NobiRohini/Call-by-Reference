#include<stdio.h>
void swap(int *,int *);
int main()
{
    int x=10,y=20;
    printf("before Swap");
    printf("%d/n%d/n",x,y);
    swap(&x,&y);
    printf("after swap");
    printf("%d%d",x,y);
}
void swap(int *x,int *y)
{
    int z;
    z=*x;
    *x=*y;
    *y=z;
}
